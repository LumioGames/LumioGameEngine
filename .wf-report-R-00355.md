# R-00355 / C-1 交回物报告 — Gameplay command envelope and Chat mapping contract freeze

状态：DONE
分支：`rm00011/c1-envelope` · commit `7a9ecc0` · worktree `C:\Work\LumioGames\LumioGameEngineArchitecture\.claude\worktrees\agent-a2643dc2004e68e85`（基于 origin/main d012c5c）

## ① 改动清单

| 文件 | 变更 |
| --- | --- |
| `.spec/decisions/ADR-049-replication-state-payload-and-input-command.md` | 原地定稿：Draft → **Accepted (2026-09-01)**，八段齐全；显式处理 ADR-052 记录的「V1.5 路线未采纳」（迁移节：语义不变、载体换成开发态 wire 契约）；清除失效的 schemas/README、V1.5 batch、tools/lumio_contract.py 引用；§4 按 Room Review Ruling 收窄（移除客户端 commandSequence/tickId/predictionKey，无 sender 字段——发送者经公共绑定解析）；§5 Chat 映射三件 + 有界输入 freeze（reject 单一行为）；§6 挖方块纸面套验（voxel-dig 12 字节 worked example） |
| `engine/wire/gameplay-command-envelope-v1.json` | 新建 `lumio.gameplay-envelope.v1`：messages（InputCommand/FullSnapshot/Delta/Error）、mappings 注册表（chat.input / chat.event / chat.component，各带 persistence/replication/visibility 维度声明）、blockRules、boundedInput（text ≤512 UTF-8 字节、每发送者每 tick 1 条、policy=reject）、errorCodes 12 个、limits、encoding（ADR-047 LumioBinV1）、真实 hash examples（程序生成并经校验器重算）、rules 12 条（validator/receiver 分级）、testCases 4 正例 + invalidCases 9 负例（7 条 validator 可判 + 2 条 receiver 语义探针） |
| `eng/verify-wire.mjs` | 新建统一 wire 契约校验器：自动发现 engine/wire/*.json；结构语法 + 引用完整性（错误码/规则 id 可解析）+ 块语义（sha256 重算先于解释、LumioBinV1 解码+重编码逐字节一致、字段上限、块 kind 规则、mappingId 严格升序唯一）+ 内嵌用例执行（validatorCheck:true 必须以声明错误码被拒；false 校验声明完备性）。hello-wire-v1 无内嵌用例走结构层原样通过 |

未触碰（按文件集边界）：`.spec/decisions/README.md`、`docs/adr/`、`engine/wire/README.md`、`engine/wire/hello-wire-v1.json`、`eng/verify-hello-wire.mjs`、`engine/abi/**`。

## ② 验证证据（真实命令输出）

- `node .spec/tools/spec-lint.mjs` → `spec-lint: OK`（exit 0）
- `node eng/generate-abi.mjs` → `ABI_VERSION=1 / ENTRY_SYMBOL=lumio_engine_get_api_v1 / DEFINITION_SHA256=1dfc86…`，`git status` 无生成物差异（**零差异达成**）
- `node eng/verify-hello-wire.mjs` → `tests 9 / pass 9 / fail 0`（既有契约未被破坏）
- `node eng/verify-wire.mjs` → `gameplay-command-envelope-v1.json: OK (cases executed: 13, clean passes: 6, problems: 0)` + `hello-wire-v1.json: OK` + `verify-wire: all contracts green`（exit 0）
- **违约探针（对照组纪律）**：向 engine/wire/ 投入 `probe-broken-contract.json`（重复错误码、坏 contractId、坏 dir、引用不存在的错误码/规则）→ 校验器 exit 1，6 项问题逐条输出（如 `errorCodes contains duplicates`、`expectedRejection not_a_code not in errorCodes`）；删除探针 → 恢复全绿。探针还暴露并修复了校验器一个真缺陷（语义层数组未守卫时 internal error 而非干净拒绝——已加 Array.isArray 守卫后复跑探针，输出全部为干净拒绝）。
- 开发中另一处真不一致被自己的负例抓到：块类型原把 mappingId 声明为 `enum:mappings`，未知映射在语法层以 bad_envelope 被拒、抢了 `unknown_command_type` 专属码——已改为语义层注册检查（CommandBlock/StateBlock.mappingId 降为 string + blockRules 注明），负例以声明错误码通过。
- dev-run.ps1 未在 worktree 执行（依赖 `..\LumioServer` 等兄弟仓相对路径，worktree 下断链）——按简报由主 loop 在主工作区串行合并时统一执行。

## ③ Known gaps

1. `.spec/decisions/README.md` 索引中 ADR-049 状态行仍写 Draft——按文件集边界留待主 loop 串行合并时统一登记（与本 ADR 文件状态短暂不一致，合并时消解）。
2. `engine/wire/README.md` 未加新契约条目——同上，主 loop 合并时登记。
3. 有界输入速率规则（每发送者每 tick 1 条）与 roomSequence 严格递增是 receiver 侧语义，校验器只验证声明完备——运行期强制归实现卡（R-00351 等），契约 invalidCases 已带运行期探针描述。
4. u64 在 JSON wire 上限 2^53-1（已在 encoding 节声明）；真 u64 宿主侧需 adapter 边界检查。

## ④ 知识沉淀落点

决策 → ADR-049（本卡产物）；契约机制（engine/wire 每契约一 JSON + verify-wire 统一校验 + 内嵌正反用例 + validator/receiver 分级）如成为后续三张 C 卡与正式硬化的固定模式，建议主 loop 合并后经 spec-steward 沉淀进 `knowledge/standards/repository-architecture.md`（开发态契约变更顺序的现实化描述）。本 worker 按边界未动 knowledge/。
