# R-00357 / C-3 交回报告 — Account Auth/Profile Port and admission contract freeze

- Status: DONE
- 分支：`rm00011/c3-account-port`（基点 d012c5c）
- Worktree：`C:\Work\LumioGames\LumioGameEngineArchitecture\.claude\worktrees\agent-adfa36696ef8d195a`
- Commit：`76ae971` — docs(decisions,wire): record account server topology and freeze account port contract (R-00357/C-3)
- 日期：2026-09-01

## ① 改动清单（2 文件，+466 行）

| 文件 | 性质 |
| --- | --- |
| `.spec/decisions/ADR-054-account-server-topology-and-port.md` | 新增 ADR（**编号 054 为暂定占号**，主 loop 串行合并时双目录现查后定最终号；Accepted 状态行已写 2026-09-01 + Room Review Rulings #3/#3b/#5）。八段齐全；引用 ADR-004/042/047/052 均为同目录实链接。 |
| `engine/wire/account-port-v1.json` | 新增契约（contractId `lumio.account-port.v1`，version 1）。sections：purpose/rulings/roles/topology/transport/identity/operations/admissionCredential/botToolCredential/takeover/passwordProfile/errorCodes(14)/errorCodeSemantics/limits(8)/process/fixturesNote/testCases(7)/invalidCases(11)。 |

要点对照卡内 Requirements：
1. 拓扑 ADR ✓（独立 C# 进程 LumioServer/account-server/、持久账号库硬性要求、低频 ECS AccountEntity World、凭证材料规则「哈希落盘 / 绝不返回 / 绝不进普通组件」）。
2. login-or-register 字段+失败码 ✓（幂等创建 / wrong_password 零覆写 / 并发首次收敛同一 AccountId / 不预置账号）。
3. 签名过期不透明准入凭证 ✓（LumioBinV1 payload + LumioSignatureV1 签名，trustDomain `account-admission`；Game Server 离线验证端口 verify_admission；密钥材料不入契约）。
4. Bot 工具真凭证 claim ✓（LumioSignatureV1，trustDomain `bot-tool`，scope `bot-namespace`；`^Bot[0-9]+$` 命名空间四触点失败码：register / claim(malformed·invalid·expired) / login（含口令正确的已存在 Bot 账号，P1-5 路径）/ admission）。
5. 顶号单行为 ✓（TakeoverNotice 形状+语义在本契约冻结：reasonCode=connection_superseded + reconnectEligible + issuedAt；信封注册归 R-00355 契约承载，已显式声明避免双真值）。

## ② 验证证据（真实输出摘录；全文见 worktree `.wf-evidence-r00357.txt`）

```
$ node .spec/tools/spec-lint.mjs
spec-lint: 1 处不一致
  ✗ .spec\decisions\ADR-054-account-server-topology-and-port.md: 未登记进 decisions/README.md 索引
spec-lint exit=1
```
→ 恰好 1 条且为**预期**：按文件集边界本卡不改 decisions/README.md（四卡并行防冲突），登记由主 loop 串行合并时统一做，登记后该项消除；其余校验项全部通过。

```
$ node eng/generate-abi.mjs     → ABI_VERSION=1 / ENTRY_SYMBOL=lumio_engine_get_api_v1 / DEFINITION_SHA256=1dfc86…  exit=0
$ git status --short engine/abi engine/native engine/managed   → （无输出 = 生成物零差异）
$ node eng/verify-hello-wire.mjs → tests 9 / pass 9 / fail 0 / cancelled 0 / skipped 0   exit=0
```

```
$ node .wf-selfcheck-r00357.mjs
errorCodes: 14, errorCodeSemantics: 14 / testCases: 7, invalidCases: 11
SELF-CHECK OK        exit=0
$ node .wf-selfcheck-r00357.mjs engine/wire/account-port-v1.json --probe
PROBE: 3 detection(s) expected >0
  DETECTED: errorCodeSemantics key not in errorCodes: bot_tool_credential_expired
  DETECTED: operations.login_or_register.failureCodes unknown code: …
  DETECTED: invalidCase …expectedRejection not in errorCodes: …
probe exit=0
```
→ 自检含引用完整性（失败码三处引用一致）、验收项 2 的正负用例完备性检查（bot-claim 与 takeover 各正例+负例）、P1-5 用例存在性、restart-stability 用例存在性；破坏探针证明自检器能拒绝坏契约。

- dev-run.ps1 未在 worktree 执行（兄弟仓相对路径在 worktree 下断链）；按简报由主 loop 在主工作区串行合并时统一执行。

## ③ Known gaps / 偏离

1. **ADR 编号 054 暂定**：合并时主 loop 双目录（.spec/decisions ∪ docs/adr）现查最高号再定；若变动需同步改文件名、标题行与 decisions/README 登记行。
2. **decisions/README.md 索引与 docs/adr symlink 未登记**：按边界留待主 loop 合并时处理（含顺带修复既有缺失的 ADR-045/ADR-052 链接）。
3. spec-lint 因此呈 1 条预期报错（见②）。
4. ADR-042/047 仍为 Draft：本契约是它们的正式消费者之一，ADR-054 已在文中如实标注其状态；若二者未来被取代，签名/编码语义经新 ADR 取代时本契约需跟随评审（已在 ADR 兼容影响节声明）。
5. 契约的统一机器校验（eng/verify-wire.mjs）由 C-1 并行交付；本卡 worktree 无该脚本，合并时纳入统一校验（简报既定安排）。

## ④ 沉淀落点

- 决策：`.spec/decisions/ADR-054-account-server-topology-and-port.md`（本卡即决策落盘，无需另沉淀）。
- 契约真值：`engine/wire/account-port-v1.json`。
- 无需额外 knowledge/ 沉淀（纯契约交付，模式沿 ADR-052 先例）。
