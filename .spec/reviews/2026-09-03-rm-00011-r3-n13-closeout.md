---
name: 2026-09-03-rm-00011-r3-n13-closeout
description: RM-00011 r3 N-13 独立深审——对照 ADR-056 六项 Fixture 与五仓 grep 复核 N-01…N-12；放行 Accepted
metadata:
  type: doc
  status: 已交付
---

# RM-00011 r3 N-13 独立深审 closeout（2026-09-03）

Reviewer：独立深审（写的人 ≠ 审的人）。无 Workflow token。未改 entity_chat 实现。未 `--admin`。未把 not-ok 写成 SUCCESS。
审查环境：Arch `wt-arch/r-00377-closeout` @ `f95a670c308c4596ffde9cb165df39d1fa75d9ca`；Server ADR `wt-server/r-00377-adr` @ `3c65343ab8c6970875fdc27a68d52c9371838933`；live10 只读 `wt-game/r-00376-r3/integration/entity-chat/evidence/n12-live10/`。
裁决落点：[`../decisions/ADR-056-rm00011-architecture-convergence.md`](../decisions/ADR-056-rm00011-architecture-convergence.md) Draft → Accepted（本卡）。

## 结论

**通过。** P0=0，P1=0。ADR-056「验证 Fixture」六项均有本机命令输出；五仓交付路径无第二份绑定表 / 声明表 / 定时内核 / 快照旁路 / 事件队列。允许 ADR-056 定稿 Accepted，Server ADR 0006 由 0008 取代。

未把下列 known gaps 写成 SUCCESS：11-scenario CI `FAILURE`；MVP C# host policy `FAILURE`（CS0234）；C# `mvp-host/` 仍含 r2 绑定表/ITimerService（冻结对照，非交付面）。

## 覆盖声明（深审全维）

| 维 | 结论 | 说明 |
|---|---|---|
| 1 验收标准 | 过 | N-13 四条：六项 Fixture 命令输出；五仓 grep；ADR-056 Accepted + 0006 处置；spec-lint |
| 2 正确性 | 过 | 对照 ADR-056 §1–§8 与 N-01…N-12 合入 SHA；live10 四轮 11/11；`--dir` 两包 `ok:true` |
| 3 安全 | 过 | 口令仅测试常量 `123456`；无新公网面；无密钥入库 |
| 4 护栏 | 过 | 未改实现仓；未放宽 Fixture；未假冒 `lumio-mvp-host`；未合成 eventOrder |
| 5 测试 | 过 | `node --test verify-evidence.mjs` 60 pass；`--dir` pack-a/pack-b exit 0 |
| 6 提交卫生 | 过 | 本卡只改 Arch `.spec` 与 Server `.spec/decisions` |
| 7 沉淀 | 过 | 决策落 ADR-056 Accepted 与 Server 0008；本报告落 `reviews/` |

## N-01…N-12 对照（相对 ADR-056）

合入真值（`ls-remote origin/main` 2026-09-03）：

| N | 卡 | 仓 origin/main | 对照 |
|---|---|---|---|
| N-03 | R-00365 | Arch `f95a670` PR #68 | C-4′ 单核双模式；`native-abi.json` 14 个 `timer_*`（含额外 3 个 scope/close 投影，Owner 已知非 P1） |
| N-04 | R-00366 | Runtime 生成表随后续 PR 在 `ff70401` | 标注类型 + 生成器；产物 1153 B LF sha256 `a47e92d6…`；6 行表（含 `claimedMark`/`unmappedMark`） |
| N-01 | R-00367 | Arch C-2′ 已合入 `f95a670` 祖先 | Runtime 发号、五元组、内嵌生成表 |
| N-02 | R-00368 | Arch `997bcf3` PR #67 | `stateBlocks` + `ConnectionSuperseded` + `entity.identity` kind=state |
| N-05 | R-00369 | Runtime PR #26 `5db2a55` | `Admit` 发号；`DeclareDefaults` 加载生成表 |
| N-06 | R-00370 | Runtime `f28709e` + persist `ff70401` | Ingress→CommandBuffer；Spawn occupancy 仍 P2 |
| N-07 | R-00371 | Runtime PR #27 `77a387b` + `ff70401` 预算 | `EcsPersistSnapshotPipeline` 公开落盘 |
| N-08 | R-00372 | NativeCore `29eaa47` PR #6 | `timer_*` FFI；`HostTimerService` 源码已删 |
| N-09 | R-00373 | Game `6b058fe` PR #9 | `ChatRoomWorld`/`ChatComponentSchema`/`GameRoomHost` 生产源删除；csproj 引用 Runtime |
| N-10 | R-00374 | Server `3c65343` PR #30 | Rust 宿主消费 Runtime+NativeCore；Room 广播；顶号上网线 |
| N-11 | R-00375 | Client `780bc5c` PR #15 | ReplicaWorld 按 `stateBlocks` 重建；Bot `ClientTimerManager`；session 自动重连 P2 |
| N-12 | R-00376 | Game `8e7d9bc` PR #10 | live10 两包两轮；oracle `d05f0458…`；`compareRuns` 按 roomSequence 文本多重集 |

## 验证 Fixture（六项，本机命令）

### 1 依赖方向 — **pass**

```text
git -C wt-game/r-00376-r3 grep Lumio.GameRuntime.Ecs|Replication -- *.csproj
# Lumio.Game.ServerGameplay.csproj:19-20 ProjectReference Ecs + Replication
git -C wt-server/r-00377-adr grep Lumio.GameRuntime -- entity-chat-host
# HostEntry.cs 动态 Assembly.Load Lumio.GameRuntime.*.dll；Admit/Query/Persist 经 Runtime
git -C wt-client/r-00375-r2-timer grep Lumio.GameRuntime -- *.csproj
# 零 ProjectReference（Client ReplicaWorld 消费 C-1，不跑权威 EcsWorld）— P2
```

交付宿主 `modules/process/src/entity_chat` 无 `by_account` / `next_net_entity_id` / `expire_due` / `read_attribute`（architecture 测试禁词）。`mvp-host/` 仍有 `RoomAdmissionRegistry` / `ITimerService` / `TakeoverNotice` / `ChatRoomWorldAdapter`：冻结对照，非 SUCCESS 路径。

### 2 标注生成 — **pass**

```text
git cat-file blob origin/main:modules/ecs/generated/attribute-declarations.json
bytes=1153 sha256=a47e92d663ba8f9726cf8defdacf2f56ebbaf1b93a8be9b7435430fad48bddc0 crlf=false
C-2 attributeDeclarations.sha256 同值；C-1 generatedAttributeDeclarations.sha256 同值
JSON.stringify(C-2.table) === JSON.stringify(Runtime 6 行) → true
```

6 行（ChatComponent×3 + EntityIdentity.entityType/unmappedMark/claimedMark）是 N-04 产物，不是第二份手写表。

### 3 广播 — **pass**

```text
node integration/entity-chat/verify-evidence.mjs --dir .../n12-live10/pack-a  # exit 0 ok:true failures:[]
node integration/entity-chat/verify-evidence.mjs --dir .../n12-live10/pack-b  # exit 0 ok:true failures:[]
node --test integration/entity-chat/verify-evidence.mjs                      # 60 pass / 0 fail
```

四轮 Playwright `ran=true` `browser=chromium` `receivedFromNetwork=true` `injected=false`；`windowLines.length=101`；`roomSequence` 1→101 严格递增；末条 `hello-browser`。`compareRuns` 比较 101 文本多重集 + 递增 roomSequence，不把 Bot.Host 发送排列写成失败；`-x`/空/`hello-browser` 缺失仍 FAIL。

### 4 快照 — **pass**

四轮 `traces.persist.processA.pid ≠ processB.pid`，均为 `lumio-entity-chat-replay`；snapshot 58340 bytes；`historyCountMax=0`；`clientWindowAfterRestore=0`；`windowBeforeSnapshot=101`。suite 对 browser 实体 `ChatComponent.lastMessageText` 断言 `last_after.value == last_before.value`。persist sha256：三轮 `b26ac745…`，pack-b/r1 `91bd6397…`（内容差，非伪造）。

### 5 定时 — **pass**

四轮 `client-bots/timer-trace.json`：`tickSource=native-kernel/tickFrame` `utteranceTicks=[5,10,15]` `process=Lumio.Client.Bot.Host`（独立 pid）。S9：`disconnect` → `clock.advance_ms(RECONNECT_WINDOW_MS+1000)` → `drive_kernel()` → `kernel.pump_wall_clock` 回调 `DISPATCH_EXPIRE`；生产源无 `expire_due`。NativeCore 无 `HostTimerService` 实现。live `traces.expiry` 未写入 `KernelFired` 记录（P2，路径仍是内核 pump）。

### 6 顶号 — **pass**

四轮 `connectionSupersededReceived=true`；`oldConnectionId=c-bot100`；重绑同一 32-hex `000…64`。suite 在旧 `RoomClient` 上 `recv_text()` 断言帧含 `"messageType":"ConnectionSuperseded"` 后再 `is_closed_after()`。浏览器页未被顶号（`browser-result.connectionSuperseded=false`）符合场景。

## 五仓 grep（交付路径）

命令：`git grep` 符号表 `ChatRoomWorld|ChatComponentSchema|GameRoomHost|RoomAdmissionRegistry|TakeoverNotice|expire_due|HostTimerService|next_net_entity_id|by_account|DeclareDefaults|ITimerService|Queue<PendingInput>|CapturePersistState`。

| 仓 | SHA | 交付路径 |
|---|---|---|
| Game | `8e7d9bc` | 生产源 0。测试类名 `ChatComponentSchemaTests` 已改为扫 N-04 标注 |
| Server 交付 | `3c65343` `modules/process`+`entity-chat-host`+`host-runtime` | `expire_due`/`by_account`/`next_net_entity_id` 仅 architecture **禁词测试** |
| Server `mvp-host/` | 同 SHA | **有** `RoomAdmissionRegistry`/`ITimerService`/`TakeoverNotice`/`ChatRoomWorldAdapter` — 冻结对照 |
| Client | `780bc5c` | 生产 replica/bot 0 |
| Runtime | `ff70401` | `DeclareDefaults()` 只 `AttributeDeclarationCatalog.LoadEmbedded()` |
| NativeCore | `29eaa47` | 实现 0；wire 文档禁语提到旧名 |

## Findings

### P0 / P1

无。

### P2（不阻塞 Accepted；不得写成 SUCCESS）

1. **C-4 额外 3 个 ABI 函数**（`timer_register_scope` / `timer_teardown_scope` / `timer_close_slot`）— N-03 Owner 注记，C-4′ 写明是既有 TimerScope/CallbackSlot 投影。
2. **声明表 6 行** — N-04 生成物含 `claimedMark`/`unmappedMark`；C-1/C-2 已钉 sha。
3. **TakeoverNotice vs ConnectionSuperseded** — C-3/Account 与冻结 `mvp-host` 仍用 TakeoverNotice；Room 交付面用 C-1 `ConnectionSuperseded`。
4. **Spawn occupancy** — N-06 P2：未 Attach 的 Spawn 不进 FullSnapshot。
5. **Client session 自动重连** — N-11 P2：`ClientSession.HandleDisconnect` 仍 `StartGeneration`；replica/bot 已停输入。
6. **11-scenario CI** — GitHub run `33697315715` @ `3c65343` job `Cargo entity-chat 11-scenario` **failure**（`continue-on-error: true`）。**不是 SUCCESS。** 活证据 11/11 与 required `Cargo entity-chat acceptance` success 是另一条。
7. **MVP C# host policy CS0234** — 同 run job `MVP C# host policy` **failure**。本卡冻结：C# 仍为 reference，不修 CS0234、不把该 job 写成绿。
8. **Client 对 Runtime ECS/Replication 零 ProjectReference** — ReplicaWorld 消费 C-1，不跑权威世界。
9. **S7 lastMessageText** 套件比对 browser 实体，不是 101 实体逐条 dump。
10. **live `traces.expiry` 无 KernelFired 字段** — 到期路径仍是 `pump_wall_clock`。
11. **Game `.spec/knowledge` 残留** — `chat-component.md` 仍写 `ChatRoomWorld.AdmitChatInput`；`testing.md` SUCCESS census 仍写 `nent_*`（活 census 是 32-hex）。

## 证据核验（本机复跑）

```text
node --test C:\Work\LumioGames\wt-game\r-00376-r3\integration\entity-chat\verify-evidence.mjs
ℹ tests 60 / pass 60 / fail 0

node ...\verify-evidence.mjs --dir ...\n12-live10\pack-a   # exit 0 ok:true
node ...\verify-evidence.mjs --dir ...\n12-live10\pack-b   # exit 0 ok:true

oracle file sha256 = d05f04583d637e5e041fa5ff77030a1db7c710be37c0744a3c2dc9039f3be677
四轮 evidence.oracleSha256 同值

node .spec/tools/spec-lint.mjs   # spec-lint: OK（Arch，Accepted 提交前/后各一次）
```

未重跑 launcher（Worker 未在本轮重跑；gitignore 包级 `verify-report.json` 仍是 restamp 前的 `ok:false`）。`--dir` 读 restamp 后的 `evidence.json`。未把过期 report 改写成 SUCCESS。

## 方案疑虑

- `mvp-host/` 按 0008 继续冻结；CS0234 与 11-scenario CI 红留给后续 Rust 主线，不在本卡修实现。
- 顶号后 Client session 自动重连若接到 `HandleDisconnect`，仍可能绕过 replica 停输入（N-11 P2）。
- Spawn occupancy 若产品要 Binding 全集普查，需另开 Binding 卡。

## R-00345 评论草稿（Dispatcher POST；本卡不写 Workflow）

```text
RM-00011 r3 closeout (R-00377 / N-13)

Independent deep review of N-01…N-12 vs ADR-056: 通过 (P0=0 P1=0).
ADR-056 Draft → Accepted (Arch feat/r-00377-adr-accepted).
Server ADR 0006 → superseded by 0008: C# mvp-host remains frozen reference;
freeze conditions now met because Rust lumio-entity-chat-replay passed ADR-056 fixtures.

Fixtures (this machine):
1. deps: PASS (Game ProjectReference; Server HostEntry Load Runtime; Client replica P2 zero csproj)
2. declarations: PASS sha256 a47e92d663ba8f9726cf8defdacf2f56ebbaf1b93a8be9b7435430fad48bddc0
3. broadcast: PASS --dir pack-a/pack-b ok:true; window 101 roomSequence 1..101 two packs
4. persist: PASS A≠B 58340B historyCount=0 lastMessageText equal (browser entity)
5. timer: PASS ClientTimerManager ticks 5,10,15 native-kernel/tickFrame; S9 kernel pump_wall_clock
6. takeover: PASS old socket recv ConnectionSuperseded then close

Five-repo grep: delivery paths clean. mvp-host still has RoomAdmissionRegistry/ITimerService/TakeoverNotice (frozen, not SUCCESS).

Honest not-SUCCESS:
- GitHub 3c65343 Repository Policy: MVP C# host policy FAILURE (CS0234); Cargo entity-chat 11-scenario FAILURE (continue-on-error). Cargo entity-chat acceptance SUCCESS.
- live10 launcher pack-level verify-report.json still shows old event-order-compare FAIL; current oracle --dir is ok:true.

Do not encode those CI jobs as SUCCESS.
```
