# 测试发现清单（R-00266）——必须能发现到测试的 package。
#
# 判据是**集合相等**，不是子集：
#   · 登记在册但发现到 0 个测试  → 发现失效（或测试被删）→ 门禁失败
#   · 发现到测试但没登记         → 清单过期、该包处于无守护状态 → 门禁失败
# 只用子集会让清单悄悄腐烂——那正是本守护要防的那类「静默」。
#
# 因此新包**第一次**有测试时必须在这里加一行；这是一次性动作，日常加测试不动本文件。
# 反过来，某包最后一个测试被删时也必须在这里删掉那行，并在交付里说明为什么。
#
# 本文件只登记 package 名，不登记测试数量：
#   数量门槛会让每次加测试都要改数，人只会把数改小，守护随之失效；
#   「>0」是能长期守住、且恰好覆盖「发现整体失效」这一失败模式的判据。
#
# 校验器：tools/verify-test-discovery.sh（由 `just nextest` 前置调用）。

lumio-core-composition
lumio-core-contracts
lumio-core-platform-contracts
lumio-core-root-abi
lumio-core-root-abi-generator
