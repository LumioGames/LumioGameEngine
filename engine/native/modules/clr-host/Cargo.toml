[package]
name = "lumio-clr-host"
description = "CoreCLR 装载链：hostfxr 手写 FFI、托管入口解析与字节协议转发（MS-00002 Wave 2；SDK 内唯一 CLR 装载实现，不引入第二套 Loader/ABI）。"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

# 运行时 crate：零第三方依赖（hostfxr/kernel32 FFI 全部手写），上层仅经 sdk-native 根表消费。
[dependencies]
