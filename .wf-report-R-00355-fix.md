# R-00355 / C-1 P1-1 fix

Worktree: `C:\Work\LumioGames\LumioGameEngineArchitecture\.claude\worktrees\agent-a2643dc2004e68e85`
Branch: `rm00011/c1-envelope`
Base: `7a9ecc0`

Closed P1-1 only. P2 (index/README/extra-member fixture/maxBlocks) not touched.

## Change

- `engine/wire/gameplay-command-envelope-v1.json`: `invalidCases.input/registered-wrong-kind` (`validatorCheck: true`, `violates: command-kind-match`, `expectedRejection: unknown_command_type`, InputCommand + registered `chat.event`).
- `eng/verify-wire.mjs` `checkBlock`: kind-mismatch throw uses `context.unknownCode` (InputCommand → `unknown_command_type`; FullSnapshot/Delta keep `state_block_kind_mismatch`).

## TDD RED

```text
$ node eng/verify-wire.mjs
verify-wire: 2 contract(s) in engine/wire/
verify-wire: FAILED
gameplay-command-envelope-v1.json: FAIL (cases executed: 14, clean passes: 6, problems: 1)
  - gameplay-command-envelope-v1.json invalidCases.input/registered-wrong-kind: rejected with [state_block_kind_mismatch] but expected [unknown_command_type] (commands[0]: mappingId chat.event has kind=event, allowed here: command)
hello-wire-v1.json: OK (cases executed: 0, clean passes: 0, problems: 0)
```

Exit 1. Failure is the expected code split, not a missing case or parse error.

## TDD GREEN

```text
$ node eng/verify-wire.mjs
verify-wire: 2 contract(s) in engine/wire/
gameplay-command-envelope-v1.json: OK (cases executed: 14, clean passes: 6, problems: 0)
hello-wire-v1.json: OK (cases executed: 0, clean passes: 0, problems: 0)
verify-wire: all contracts green
```

Exit 0.

```text
$ node --test eng/verify-hello-wire.mjs
CONTRACT_ID=lumio.hello-wire.v1
MESSAGES=8
ERROR_CODES=12
HELLO_WIRE_OK
✔ contract file loads and validates
✔ valid InputCommand passes validation
✔ InputCommand missing payloadSha256 is rejected
✔ unknown messageType maps to unknown_mapping
✔ unknown role is rejected
✔ Delta with structurally valid hex but mismatched payload hash stays shape-valid (hash equality is a consumer check)
✔ contract shape rejects mutated const field (red probe)
✔ contract shape rejects broken hash example (red probe)
✔ contract shape rejects unknown shared type reference (red probe)
ℹ tests 9
ℹ pass 9
ℹ fail 0
```

```text
$ node eng/generate-abi.mjs
ABI_VERSION=1
ENTRY_SYMBOL=lumio_engine_get_api_v1
DEFINITION_SHA256=1dfc86dad1ebbd8d6196d16946a9eb8542e951c83fa5e6163f696abee831fb8e
```

`git diff -- engine/abi` empty. `engine/abi/native-abi.json` SHA-256 unchanged.

```text
$ node .spec/tools/spec-lint.mjs
spec-lint: OK
```

## Files staged

- `eng/verify-wire.mjs`
- `engine/wire/gameplay-command-envelope-v1.json`

Not staged: this report, `.wf-report-R-00355.md`. README/index/docs/adr/hello-wire/abi untouched.

## Knowledge

Pure validator/error-code alignment with an existing rule. No new pattern. spec-steward skipped (pure fix exemption).
