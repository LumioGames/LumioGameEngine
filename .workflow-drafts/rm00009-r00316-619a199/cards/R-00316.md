# R-00316 delivery evidence

Target: R-00316 / 01a051bb-764d-76d5-b13e-a1e8b562c89e

## Delivery

- Executor: architecture coordinator
- Time: 2026-08-31 (Asia/Shanghai)
- Delivery location: `LumioGames/LumioGameEngineArchitecture`, integration branch `Go1c/rm-00009-integration`
- Integrated HEAD: `619a199715d3ef3ad3d461170f33db63a3926ed6`
- Reviewed candidate: `512da155f6480a9989f6f01815bd1e0aa83f9b54`
- Reviewed range: `23b9e019e3254ad3830b86b3fc6a8fd7cb22560e..512da155f6480a9989f6f01815bd1e0aa83f9b54`
- Candidate worktree: `C:/Users/g923/orca/workspaces/LumioGameEngineArchitecture/rm-00009-r00316`

## Change list

- Added Draft ADR-051 and its architecture mirror for LumioConfig namespace ownership, permanent numeric IDs, revision/column ordinal rules, serial allocation, rename compatibility, and tombstones.
- Added ID registry and tombstone schemas, positive/negative fixtures, registry/index entries, focused validator tests, and regenerated Rust/C# contract packages.
- Closed the independent review-v1 findings: bidirectional allocation/history links, alias-aware column endpoint validation, closed persistence declarations, order-independent tombstone replacement checks, tombstone-only snapshots, fail-closed malformed collections, and distinct duplicate-identity diagnostics.
- No production allocator, editor, runtime loader, Workflow acceptance-item mutation, push, PR, release, or protected-branch operation was performed.

## Submission numbers

- Architecture candidate commit: `512da155f6480a9989f6f01815bd1e0aa83f9b54`.
- Integration commits: `acdaff30993fedcc0e2726c9a23d01f066075e15` (initial ADR-051) and `619a199715d3ef3ad3d461170f33db63a3926ed6` (review repair).
- Independent review report: `C:/Users/g923/orca/reports/lumio-adr051-review-v2-512da15.md`.
- Review report SHA-256: `7D9DD4AC4E936662099EBCF6BD0DFFF6A64BAD2B28B0DE5717AC31E87C3430DF`.
- Repair evidence: `C:/Temp/R-00316-fix-report.md`.
- Repair evidence SHA-256: `22FFD6DFAA20CB10B85AB01A16252FEF047271FABABE960224F9B7474EE2AAE3`.

## Acceptance mapping

1. **Namespace authorization:** ADR-051, `ids/index.json`, and the valid registry mirror uniquely register LumioConfig with NamespaceOwner / DomainAutonomousSerial authority, inclusive range 40000..49999, Never reuse, and Architecture spot-check ownership. Semantic drift and out-of-band IDs are rejected.
2. **Identity semantics:** source names and aliases are readable rename handles; stable numeric IDs are permanent persisted identities; `revisionOrdinal` is ephemeral; column ordinals survive rename/reorder. The schema and semantic gate enforce the closed stable/ephemeral persistence declarations and reject row/tombstone ordinals.
3. **Allocation and compatibility:** merge-gate assignments are one-to-one with active rows and AddRow history; rename/delete/column-rename operations are cross-linked; assignments are strictly ordered and tombstone replacements consume the complete set independent of list order. Recreated rows receive new numbers.
4. **Stable rejection and reproducibility:** negative fixtures cover duplicate, out-of-range, preallocated, unlinked-history, unknown-column, malformed-persistence, ordinal-persistence, tombstone-reuse, replacement-order, and duplicate-identity cases. The complete Contract Gate and clean generated consumer surface pass.

## Fresh coordinator verification

All commands below were run in clean symlink-enabled materialization `C:/Temp/lumio-rm00009-integration-gate-619a199` at integrated HEAD `619a199`:

- `node .spec/tools/spec-lint.mjs` -> `spec-lint: OK`.
- `node --test .spec/tools/spec-lint.test.mjs` -> `13` passed, `0` failed.
- `python3 -m pip install --disable-pip-version-check -r requirements-dev.txt` -> requirements already satisfied.
- `python3 -m py_compile tools/lumio_contract.py tools/lumio_generate.py tools/config_id_test.py tools/config_patch_test.py` -> exit `0` (rerun serially after an avoided `__pycache__` contention race).
- `python3 -m unittest tools.config_id_test tools.config_patch_test` -> `54` tests, `54` passed.
- `python3 tools/lumio_contract.py validate` -> `Validated 240 fixture(s), 0 failure(s)`.
- `python3 tools/lumio_contract.py generate --out C:/Temp/lumio-rm00009-integration-generated-619a199` -> `12` artifacts, stable outputHash, compilerHash `127e7165564bb1f6b6384bdada551d49b2794c34276e490b70e30f4aeb27661d`, inputHash `49fb34ea7430a192459b5b409dda457772fa1f2d71aa9410fccf40b832d4588b`, Root ABI digest `9347330956ca1a0d9e0175c070a82e7057f8765d93c2430dda74dde7f20211c5`.
- Generated parity comparison -> `70` committed and `70` generated files; `0` missing, extra, or mismatched.
- `cargo check --manifest-path packages/rust/Cargo.toml --offline` -> exit `0`.
- `cargo test --manifest-path packages/rust/Cargo.toml --offline` -> ContractRuntime `3` passed, `0` failed; all other unit/doc tests passed.
- `cargo clippy --manifest-path packages/rust/Cargo.toml --all-targets --offline -- -D warnings` -> exit `0`.
- Six serial `dotnet restore` and `dotnet build --no-restore --configuration Release` commands -> all six projects target netstandard2.1 and net8.0 with `0` warnings and `0` errors.
- `python3 tools/lumio_kat.py` -> C#, hashlib, and Rust agree on `3` FIPS 180-4 vectors.
- `git diff --check 23b9e019..619a199` -> exit `0`.
- Accepted ADR-007/010/033/034/050, config source/patch schemas, and config patch tests retain identical blob IDs between integration parent and integrated HEAD.
- Clean materialization remains clean after verification; `.agents/skills`, `.claude/agents`, and `.claude/skills` are symbolic links resolving into `.spec`.

## Independent review

- Review task: `task_448adf1eff40`, dispatch `ctx_4b976856db7e`.
- Verdict: **Approved**; no P0, P1, or P2 findings in the complete parent-relative range.
- Report: `C:/Users/g923/orca/reports/lumio-adr051-review-v2-512da15.md` (attached).
- The review independently reproduced closure of prior P1-1..P1-6 and P2-1 defects. The historical RED transcript remains report-only evidence and is explicitly identified as such in the attached reports.

## Decision record

- The coordinator handoff explicitly authorizes R-00316 acceptance write-back after independent review and clean gates; no additional technical decision was required.
- The four native acceptance items remain platform-controlled `not_started`; this delivery submits the evidence for the acceptance owner and does not self-mark those items.

## Known gaps and deferred work

- Native Windows worktrees with `core.symlinks=false` display tracked-link placeholders; authoritative spec-lint evidence comes from the clean `core.symlinks=true` materialization. No tracked placeholder was edited.
- ADR-051 remains Draft by design. Production allocator/editor/runtime implementation is outside this requirement and is deferred to downstream requirements.
- No additional TODO or downgrade item was identified that requires a supplemental requirement card; no supplement was created.

## Boundary

Only the architecture repository was changed. No Workflow object other than this requirement will be touched, no acceptance-item status will be changed, and no production activation or public release was performed.

## Recommended transition

`acceptance` (the current transitions endpoint reports this edge as allowed and semantically “验收中”).
