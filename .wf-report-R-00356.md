# R-00356 / C-2 交付报告（连接绑定与 NetEntityId Attribute Query 契约冻结）

- 分支：`rm00011/c2-binding-query`（基线 origin/main d012c5c）
- 状态：DONE

## 一、改动清单

| 文件 | 动作 |
| --- | --- |
| `engine/wire/entity-binding-and-query-v1.json` | 新建——契约唯一真值（contractId `lumio.entity-binding-query.v1`，version 1） |
| `.spec/decisions/ADR-053-entity-binding-and-attribute-query.md` | 新建——Accepted（2026-09-01），八段齐全 |

契约章节：identityModel（五元组语义）/ binding（record + 5 不变量 + 3 操作：selfLookup、resolveByConnection、resolveByNetEntityId）/ attributeQuery（AttributeId 文法与声明源、callerScope 二分、request/success/failure 形状）/ attributeDeclarations（声明结构 + persistence/replication/visibility 三维词表 + 4 条判定规则 + 示例声明）/ outcomes（五结局：定义、生产规则、消费义务、resolvesToReplacement=false）/ errorCodes（5 结局码 + 7 请求错码 + 语义）/ fieldSemantics / limits / wordingAudit / testCases（10）/ invalidCases（8）/ boundary。

五结局各≥1 用例（验收项 2）：non_existent=1、stale_generation=2、invisible=1、unauthorized=1、tombstoned=1，另有 ok 正例 4。宿主无关措辞审计（验收项 3）：契约内 `wordingAudit` 节 + ADR 决策第 6 条双落点。

## 二、验证证据（worktree 内实测）

1. `node -e <自检脚本>`（校验必含节/contractId/version/五结局覆盖/用例结构/码表一致性/AttributeId 文法）：
   ```
   testCases: 10 (ok: 4 outcomes: non_existent=1 stale_generation=2 invisible=1 unauthorized=1 tombstoned=1)
   invalidCases: 8
   SELF-CHECK: OK
   ```
2. `node .spec/tools/spec-lint.mjs`：
   ```
   spec-lint: 1 处不一致
     ✗ .spec\decisions\ADR-053-entity-binding-and-attribute-query.md: 未登记进 decisions/README.md 索引
   ```
   **预期中的唯一失败**：四张 Wave 0 契约卡并行，README.md 属共享文件，登记（含 docs/adr symlink、engine/wire/README.md 行）由主 loop 串行合并时统一完成（任务简报明确禁改）。登记后复跑应全绿——主 loop 合并时验证。
3. `node eng/generate-abi.mjs`：输出 ABI_VERSION=1 / ENTRY_SYMBOL=lumio_engine_get_api_v1 / DEFINITION_SHA256=1dfc86…；执行后 `git status` 无生成物改动 → **零差异**。
4. `eng/verify-hello-wire.mjs`：未运行——本卡未触碰 hello-wire 相关任何文件（只读参照），该项留主 loop 合并时例行复跑。
5. `eng/dev-run.ps1`：按任务简报不在 worktree 跑（兄弟仓相对路径断链），主 loop 主工作区合并时统一执行。

## 三、known gaps

1. decisions/README.md 索引登记 + docs/adr symlink + engine/wire/README.md 导航行——主 loop 串行合并时补（见上）。
2. ADR 编号 053 为暂定占号：合并时主 loop 以双目录并集现查最高号后定终号，若变动需机械重编号（文件名/标题/索引行；正文已按简报只引用卡号与 contractId，无 ADR 号自引）。
3. 统一校验器 `eng/verify-wire.mjs` 由 C-1（R-00355）并行交付；本卡自检为其子集，最终以统一校验器执行全部内嵌用例为准（主 loop 合并时跑）。
4. 属性声明表的具体条目（如 ChatComponent.*）不在本卡范围，随 R-00355 冻结（契约内 declarationSource 已声明该边界）。

## 四、知识沉淀落点

决策已沉淀至 `.spec/decisions/ADR-053-entity-binding-and-attribute-query.md`（本卡产物本体）；无需额外 knowledge/ 沉淀（无新流程/新模式，纯契约冻结）。
