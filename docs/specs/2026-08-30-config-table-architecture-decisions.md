# 配表管线架构定稿会 · 会中裁决流水（2026-08-30）

> 实时落盘，防对话中断。格式：模块 · 裁决 · 理由 · 落点 · 保留意见。
> 最终定稿见 `2026-08-30-config-table-architecture.md`（散会整理）。
> 会议原则（沿用 GAS 会 Owner 三条）：①第一性原理，不加需求；②奥卡姆剃刀，如无必要勿增实体；③每条需求配游戏示例+大白话。

## 裁决流水

| # | 模块 | 裁决 | 理由 | 落点 | 保留意见 |
|---|------|------|------|------|----------|
| 0a | 议程 | 时间充裕，五阶段 A–E 全过；B.2 物理格式只定决赛名单不选赢家；chunk 大小/压缩算法等具体数字不今天钉死 | 报告 R.4 已分类「第一天定死 vs 可推迟」 | 本文件 | — |
| 0b | 骨架核对 | **ADR-007/010/033/034（Accepted）寸土不动**；ADR-041/047/048（Draft）可提 refine 建议、不当场默认已改；GAS 决议 0.1/14/3c 预支的白条今天兑现 | 双方无重开动议 | — | — |
| 0c | 前提画像（Owner 口述，覆盖任务书 1.D） | **全流程 AI-native，含策划配置——AI 是配表的一等作者，现在就有需求，非前瞻；比例画像 80% AI 推进、20% 人为修正**；当前没有存量策划填表工作流，管线从零建，无 Excel 迁移包袱 | Owner 拍板 | 所有模块的设计前提；E.2 从「放最后给路线图」升级为一等公民 | 生产激活权（AI 可否直接激活生产）随 A.1 裁 |
| A.1a | 权威源形态 | **Git 规范化行式文本（TXT）为唯一权威**：schema 文件+每表数据文件（每行一条记录、带永久编号、字段顺序机器排死）+墓碑文件；无 Excel 源，Excel＝按需生成的受控视图 | Owner 主动提出「保的文件全是 TXT，AI 读写/版本管理/merge 全顺」，与报告 B.6 判断题 3 结论一致；零存量 Excel，迁移成本不存在 | 定稿阶段 A；权威源目录结构 | 具体文本拼法（JSONL 或其他）归 A.2/B.1 细节 |
| A.1b | 写路径与 Excel 回环 | **AI 和人唯一写路径＝结构化补丁**；Excel 导回≠另存为覆盖，必须走「记录生成时基版本→逐格 diff→schema 校验→补丁通道合入」；护栏理由：Excel 自动改值（前导零/15 位精度/日期化，报告 B.1 Verified）+ 并行编辑覆盖 AI 已合入补丁 | 双向自由覆盖＝split-brain（B.6：分歧点不在格式，在谁拥有最终写权限） | 定稿阶段 A；工具面「视图生成/导入」规格 | — |
| A.1c | 合入门 | **合入门＝机器门**：schema/类型/引用/投影校验全过，补丁（无论 AI 还是人提的）自动合入权威源，人不逐条审 | 80/20 画像下人逐条审＝瓶颈；机器校验兜底是 AI-native 的合入纪律 | 定稿阶段 A | — |
| A.1d | 生产激活权 | **人门拍板**：生产激活＝人看语义 diff 汇总+模拟报告后一键批量签名；开发/测试环境不设门，AI 全自动；AI 无生产激活权限 | Owner 选定「人门」；落在 ADR-010「生产签名激活」（Accepted）上；报告 R.7 同向 | 定稿阶段 A/E；硬冲突④关闭 | 将来卡脖子再按 ADR 流程开「白名单数值自动激活+自动回滚」升级路径，第一天不给 |
| A.1e | 人的修正链路 | **人不直接改文件**：人为修正＝人口述需求→AI 查表→人确认改法→AI 提补丁，走与 AI 自主改动同一条补丁通道 | Owner 口述「类似数据库：人只负责提需求和让 AI 查，查完告诉 AI 怎么改」 | 定稿阶段 A/E | 可视化编辑器（A.2c）是辅助面，不改变唯一写路径 |
| A.2a | 权威文本形态 | **表格状规范化文本**（Owner：类 Markdown 表格，不是自由文本）；三条格式纪律钉死：①四态可区分（missing/empty/null/default 拼法不同）②每行行首带永久编号③机器格式化器统一排版，CI 强制、人/AI 不手排 | 表格心智模型保留、Git diff/merge 行级可用；四态是 R.4 第一天必钉不变量（Excel 正是分不清四态才出局） | 定稿阶段 A；具体方言（Markdown 管道表还是变体）B.1 定 | 若标准 Markdown 表格无法表达四态/转义，允许定制方言，「看起来像 Markdown」即可 |
| A.2b | 逻辑与数据分界 | **暂收**：逻辑归 C# 内容代码（GAS 定稿 11c 三件套+计算钩子），数据归表，**表里无 if**（出现脚本列/分支列即打回）；复杂实体＝主表一行+子表若干行经 ref 连接（正规化子表，落 R.8 兼容改法，ADR-033 不重开） | UE 蓝图混流程与数据是其难管根源；AI 写 C# 是强项 | 定稿阶段 A；回读时可翻案 | — |
| A.3 | ID 体系 | **三层行身份拍板**：①可读名（人/AI 语境，可改名）②永久编号（合入门串行分配、永不复用、墓碑随葬；存档/网络/Replay/GAS TypeId 全认它；挂 ADR-007 registry 新增配表域命名空间，扩展不重开）③版内序号（编译产物稠密下标，每版重排，**禁止持久化**）；**补丁只写可读名不写编号**，防并行 AI 抢号 | 逐层试砍各有需求塌（改名断存档/满嘴数字/热路径变慢）；R.2「稠密序号持久化」列必炸项 | 定稿阶段 A；ADR 候选（配表 ID 命名空间） | Owner 确认；澄清：字段名(列)的稳定序号归 Schema 演进规则（A.2/R.4 字段 ordinal），不占行身份三层 |
| A.2c | 可视化编辑器 | **本地 Web 编辑器为唯一官方可视化面**（Owner 拍板，体验对标飞书文档：HTML 打开即编辑）：数据源=本地 TXT，导出=TXT；渲染平表+技能卡聚合视图；**「保存」底层＝基于打开时版本的逐格补丁**，走补丁通道，永不整文件覆盖（编辑期间 AI 可能已并行合入补丁）；**Excel 回环从 V1 砍掉**，触发条件＝出现真实 Excel 协作需求 | 自研 Web 面无 Excel 自动改值毛病、可内嵌校验与聚合视图、AI 可自建自扩；剃刀：两条视图通道砍成一条；飞书底层同为补丁流，类比自洽 | 定稿阶段 E 工具面；V1 可从只读检视器起步 | — |
| B.1a | 三重指纹 | **拍板（Owner「继续吧」确认转正）**：内容指纹（逻辑值，格式/压缩无关，热更对账/Replay/握手认它，规范形式复用 ADR-041 CanonicalJsonV1）＋包裹指纹（文件字节，下载校验/内容寻址）＋底稿指纹（源+schema，审计、区分明填与默认）；构造规则今天钉死（R.4 不变量），实现第一期即「按规则算 SHA-256」，内容寻址复用推迟 | 需求 4：换格式≠内容变；报告洞察 2 | 定稿阶段 B；ADR 候选（配表 Hash 域） | — |
| B.1b | 浮点拆弹 | **Owner 拍板采纳**：①权威数值列（进权威模拟判定的）禁 f32/f64，一律定点整数——schema 声明类型+单位/精度（千分比/帧/铜板），作者照写「5%」「2.5秒」，编译期换算（GAS 决议 3c 泛化为 Schema 通用能力）；②纯表现列允许 f32/f64，内容指纹盖 IEEE-754 位模式（按 u32/u64 整数进 hash），编译期钉死解析规则、拒 NaN/Inf、负零归一 | 整数进 CanonicalJsonV1 天然合法，ADR-041/047 零修改；ADR-047 明文「需要浮点的域自开 ADR」——配表域即该域，规则写进配表 ADR 候选；硬冲突①关闭 | 定稿阶段 B；ADR 候选 | Owner 强调「表里必须声明数据类型」——与 ADR-033（Accepted）同向，单位声明为今日新增能力 |
| B.1c | 五层合并 | ADR-010 五层顺序（Engine→Platform→Server→Product→Environment→User/Session）照抄，**编译期合并为不可变生效值+每值带出处标签（provenance）**，运行时只见合并结果 | R.8「层级覆盖」摩擦关闭；R.2 provenance 列「必须现在定义元数据」 | 定稿阶段 B | provenance 查询 UI 可推迟，元数据字段今天留 |
| B.2 预 | Luban 定位 | 总监立场在桌（Owner 未表态）：**不整套采用**——「进货」（Excel/多源解析、校验、代码生成）是其强项，「出货」（三投影、签名 Manifest、canonical Hash、Tick 边界热更、ADR-007 编号）一片空白且是我们命根；其类型系统大于 ADR-033 须收窄白名单；Rust 生成现状未核（报告 P.2）；**AI-native+无 Excel 前提下其「进货」价值也大幅缩水** | 报告 P.2 第 11/12 条：值得抄 typed IR+多端分组，不该把「后端可切换」误当「运行时语义已稳定」 | 阶段 B.2 决赛名单一并裁 | — |

| B.2 | 格式决赛名单 | **拍板**：第一阶段规范文本产物（每表一块，非全包大文件），且为**永久对账基准**（洞察 10）；第二阶段决赛名单封闭三家：①FlatBuffers+外置索引 ②自研 typed binary 以 LumioBinV1（ADR-047）为原语层（硬冲突⑤答案：钉进决赛，浮点缺口由 B.1b 补）③SQLite 只读降格为特殊表族备选；Protobuf/Parquet/rkyv 淘汰不复议；实测挂 E.3 | 报告 R.3；提名不花钱，防第四套格式冲不变量 | 定稿阶段 B | 今天不选赢家，实测定 |
| B.3 | 产物目录 | **拍板**：采纳报告附录草图为结构骨架——发布清单→三端投影清单→表描述→块描述四层+安全读取八步；字段细节实现期修，转正走配表 ADR 候选 | 内容/包裹指纹的安放处，非新概念 | 定稿阶段 B；ADR 候选 | 草图自称非规范性，转正须独立规范+测试向量 |
| B.4 | 压缩与内存 | **拍板推迟**：压缩算法/块大小/缓存容量不今天定，目录结构预留 codec 字段与块指纹字段 | R.4 可推迟清单原文 | 触发条件＝E.3 实测数据出炉 | — |

| C.1 | 三端小抄（投影）与作弊面 | **拍板（Owner 以「导出控制字段」形式确认机制）**：每列「发给谁」标签 S/C/CS/V，**不写默认 S**（漏标=客户端缺列报错，绝不外泄）；表级默认+列级覆盖；客户端小抄里机密列**根本不存在**（非加密）；跨端引用默认编译错误，确需时降级为不可解引用的编号；握手各端只核自己小抄+公共部分的指纹；**列首次标 C 必须出现在生产激活单上**（披露门禁挂人门） | 需求 5、洞察 8；R.2 disclosure 门禁「必须现在补」；泄密不可逆（进过客户端包=永远收不回） | 定稿阶段 C；Schema 可见性字段 | — |
| C.1v | Voxel 边界（硬冲突⑥） | BlockType 表（碰撞/材质引用/光照常量）**进公共 Schema/三小抄体系**（V 标签）；其在 Voxel 内部的物理打包布局维持 adapter-internal，**D-013/014 不重开**。一句话：表进体系，摆法自理 | F.7 判据：BlockType 常量是配表 | 定稿阶段 C；接缝对账 | — |
| C.2 | 双语言读表代码 | **暂收**：编译器生成 Rust/C# 只读 TryGet 视图，落 ADR-048 生成面（双目标/声明序）；业务代码只许用生成 API，禁止直接拆文件格式（换后端时业务零改动，R.4 不变量）；WASM/AOT 生成代码包体进 E.3 必测 | 报告 G/L 章；银行 App 比喻 | 定稿阶段 C | 回读时可翻案 |

| D.3 | 生产激活语义（★，Owner 画像修正后改判） | **生产＝滚动更新，无停机内热更**：一份 Revision 绑定服务器实例一生，开机装载、退役不变；Tick 边界切换自动满足（首帧前完成），ADR-010 零冲突；**生产环境换幕机从 V1 砍掉**。两条纪律不动：①一实例一版本、整套 Revision 原子（混版本雷只是拆弹时机挪到开机；滚动窗口新旧并存靠握手指纹配对，连错拒入）②签名人门不动（签的就是下一波滚动的版本） | Owner 口述运维画像；剃刀 | 定稿阶段 D；接缝对账（ADR-034） | — |
| D.3d | 开发期 reload | **拍板**：开发期强制重载走同一「原子换版本」原语（攒好新版→帧间隙整套换），跳过签名/人门；语义模板抄 ADR-034 阶段与失败规则；**生产不用它**，升级触发条件＝出现不能重启的长会话运营或紧急热修需求 | 防开发/生产语义漂移（半帧新旧的 bug 生产无法复现） | 定稿阶段 D；推迟表 | — |
| D.1 | 按需下载 | **拍板**：清单先行，表按需备；默认表级、超大表切片级；浏览器进游戏下开局包、进副本前备副本包；同一 Revision 内按需，不跨版本混拿 | 报告 D 章、洞察 3/4 | 定稿阶段 D | 切片阈值等数字实测后定（B.4 同款推迟） |
| D.2 | 回放钉版本 | **拍板**：Replay 记录当时 Revision 指纹，回放钉住该版；旧版产物按保留政策留存（防回放 404） | 报告 J.8；R.9 风险「旧 artifact 保留不足」 | 定稿阶段 D | 保留时长=运营策略，不今天定 |
| D.4 | 查表 API 纪律 | **拍板**：帧内查表（TryGet）永不发 I/O、同步、绑定当前 Revision；I/O 只发生在显式「声明+备货」（PrepareAsync/usage 声明）；实例开机/进场景前必需集备齐才放行 | 需求 7、洞察 6；防「第一次放新技能卡一秒」 | 定稿阶段 D；生成 API 形状 | — |

| E.1 | 机器门与报错 | **拍板**：机器门四关＝格式关（类型/引用/四态）→小抄关（可见性/机密引用）→语义 diff（人话：「炎魔王掉率 2%→3%」，供人门）→结构化报错（哪表哪行哪列/什么错/怎么修，**第一读者是 AI**，可自修重提） | R.2「结构化错误必须现在补」；AI-native 命门 | 定稿阶段 E；报错格式参照报告附录 validation-error-example | — |
| E.2 | AI 工具面 | **拍板**：AI 五动作封闭集＝查/提案/预检/预演/提交，**无第六动作（激活）**；审计走 Git（actor+改前后指纹+理由）；R.7 三档对号：「现在就能做」全进 V1，「先建基建」（模拟框架/表级权限）排垂直切片后，「观望」（AI 自主大范围经济平衡直接发布）**明确不做** | Owner 前提 0c 的具体兑现；硬冲突④已由 A.1d 关闭 | 定稿阶段 E | — |
| E.3 | 实测计划 | **拍板**：二进制决赛入场券四项＝百万行内存/浏览器冷启动/双语言同字节点查/生成代码包体；AI 跑、报告落仓；时点＝垂直切片后、二进制选型前 | 报告 O 章 benchmark 清单现成 | 定稿阶段 E；B.2/B.4 触发条件的供给方 | — |

| E.4 | 工具双件套 | **拍板（Owner 定形态）**：①**配表编辑器**＝本地 Web/HTML（飞书感，前期简单表格即可），源 TXT、存 TXT（即 A.2c，保存=逐格补丁）；②**导表工具**＝一键编译/一键导表，跑编译器出三份小抄+清单，分发到各实现仓库。两条纪律：导到各仓的产物是**生成物，只读、不得手改、可重建**（rules/system.md 既有硬规则+ADR-007 产物纪律）；**一键导表≠激活**——导出是上货，生产生效仍走人门签名+滚动更新 | Owner 口述「一套编辑、一套导出」 | 定稿阶段 E；跨仓分发对齐 ADR-007 产物流 | — |
| E.5 | LumioConfig 独立仓 | **拍板（Owner 定）**：配表体系落独立仓库 **LumioConfig**——装权威源（schema 实例+TXT 数据+墓碑）、Web 编辑器、导表编译器；权责三分：**架构仓管规矩**（信封 Schema/指纹与 canonical 规则/ID 命名空间授权/契约校验器，经 ADR 冻结），**LumioConfig 管内容与工具**（含在授权命名空间内自治发号——合入门串行分配，规则由架构仓 ADR 冻结、校验器抽查），**实现仓只收只读生成物** | 域内自治发号防架构仓成为每行内容的占号瓶颈（AI-native 节奏）；产物纪律沿 ADR-007 | 定稿阶段 E；建仓与镜像登记按 repository-architecture 既有规则走 | — |
| S.1 | 接缝：GAS 白条 | **全部销账**：永久编号 TypeId＝A.3 直接覆盖（GAS 零调整）；公式声明（常量/曲线/属性捕获）＝ref 指向公式表/曲线子表（正规化子表实例，不开专门「公式类型」）；fx_key/覆盖优先级/发布档位/打断策略/存档档位＝enum+ref+子表全够用；**决议 3c「策划配秒管线换帧」泛化为 Schema 单位声明通用能力**（B.1b）；决议 0.1「Excel 配表直接继承」改写为「配表管线继承」（GAS 消费的是能力不是 Excel，零影响） | 逐条对 GAS 定稿核过 | 定稿接缝章 | — |
| S.2 | 接缝：ECS 候选 9 | **边界判断（一句话）**：EntityType 声明（组件集/依赖互斥/CS-Local 模式）按 F.7 判据属**编译期类型声明**，不是「每 Revision 生成、运行时只读的数据」，**不走配表 Schema/IR 机制**；ECS 那份 ADR 不用等今天 | F.7 可操作判据 | 定稿接缝章 | 若将来 EntityType 想按 Revision 热变，再回表体系重议 |
| S.3 | 接缝：ADR-010/034 | 五层合并＝B.1c（编译期合并+出处标签）；ADR-034 映射＝D.3/D.3d（生产滚动更新砍换幕机；开发 reload 用其语义模板；「一幕」=整套 Revision 非单表） | 已在各模块裁决 | 定稿接缝章 | — |
| Z.1 | 收尾①互检 | **五阶段契约互检通过**：浮点（B.1b）与类型系统（ADR-033）一致——权威列定点即整数、表现列 f32/f64 保留、冻结类型清单未动；热更（D.3）与三小抄（C.1）一致——滚动窗口靠握手指纹配对，一实例一版本=整套 Revision 原子；四态拼法（A.2a）进内容指纹 canonical 规则（B.1a）；补丁只写名（A.1）与门房发号（A.3）闭环；开发 reload（D.3d）同样受「备齐才切」（D.4）约束 | 当场逐对检查 | 定稿收尾章 | — |
| Z.2 | 收尾②不可妥协核心 | **核心＝「Schema-first 文本权威 + 格式无关内容指纹 + 一实例一版本整套原子装载 + AI 无激活权（人门）」**；任务书预测的第三条「Tick 边界原子激活」按 Owner 运维画像修正为「整套原子装载」（滚动更新形态下的同义实现，ADR-010 骨架未动）；将来砍功能先护这四条 | 前两条已部分是 Accepted ADR 且被 GAS 消费 | 定稿收尾章 | — |
| Z.3 | 收尾③砍单清单 | 同类系统有而我们明确不做/推迟（各附「删掉它哪条需求塌」检验）：AI 自主经济平衡直接发布（不做，报告标观望）；任意 SQL 查询（不做，直到 trace 证明需要）；全局列存运行时（不做）；Excel 回环（砍，触发=真实 Excel 协作需求）；生产不停机热更（砍，触发=不能重启的长会话或紧急热修）；公共 Schema 容器类型（推迟，正规化子表顶住，触发=子表方案挡不住的真实结构需求）；Luban 整套采用（不采，AI-native+无 Excel 前提下其价值集中段已无买家）；表内逻辑/蓝图式 DSL（禁，表里无 if） | 每条都答得出「删掉它没有需求塌」或已记触发条件 | 定稿收尾章 | — |

## 待办

- [x] 阶段 A：权威源/Schema/ID
- [x] 阶段 B：编译器与产物格式
- [x] 阶段 C：三端投影与可见性
- [x] 阶段 D：运行时加载与热更（生产改判滚动更新）
- [x] 阶段 E：工具链与 AI 接口（含 E.4 双件套、E.5 LumioConfig 仓）
- [x] 接缝对账（GAS/ECS/DS/ADR-010/ADR-034）
- [x] 收尾三问 + 散会定稿整理：[`2026-08-30-config-table-architecture.md`](2026-08-30-config-table-architecture.md)
