# ADR Details (Compatibility Entry)

ADR 权威索引与正文已迁移到 [`.spec/decisions/`](../../.spec/decisions/README.md)。本目录保留历史 URL 的文件级软链接，不定义第二套内容或状态。

新增或修订公共决策时只编辑 `.spec/decisions/`，并按 Architecture Gate 同步 Schema、ID、正向/失败 Fixture、Baseline 与七个实现仓镜像。
