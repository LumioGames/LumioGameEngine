# R-00356 / C-2 审查修复报告（P1 分类优先级 + 失败形状）

- 分支：`rm00011/c2-binding-query`
- 基线提交：`b9f8a8c`（首次冻结）
- 修复提交：`b075e51` `fix(wire): close C-2 review P1 classification and failure-shape gaps (R-00356)`
- 状态：DONE

## 一、改动清单

| 文件 | 动作 |
| --- | --- |
| `engine/wire/entity-binding-and-query-v1.json` | 修审查 P1-1/P1-2 与 P2 对齐：分类顺序、双变体失败形状、存储寻址识别器、多违例单码裁决、invalidCases 对齐 |
| `.spec/decisions/ADR-053-entity-binding-and-attribute-query.md` | 失败语义改写为双变体单码；补 u64 记法豁免、多违例裁决顺序、钉死用例不得改判 |

未触碰（禁改）：`.spec/decisions/README.md`、`docs/adr/`、`engine/wire/README.md`、`hello-wire-v1.json`、`eng/`、`engine/abi/**`。

### 对照审查项

- **P1-1 错码分类无优先级**：`errorCodes.classification.order` 固定 ①存储寻址识别器 → ②文法 → ③已声明。`sql_expression_payload` 仍为 `invalid_attribute_id`；`storage_path_addressing`（`Storage.tables.entity_row(42)`）为 `storage_access_forbidden`。新增 `storage_path_separator_addressing` 覆盖第三项识别器特征 `/`。
- **P1-2 失败形状不可满足**：`attributeQuery.failure.variants` 拆成 `outcomeFailure`（仅 `outcome`∈五结局）与 `requestError`（`outcome=request_error` + `code`∈requestErrorCodes + `detail`）。两码表不相交；`invalidCases.expectedRejection` 单码即可判定变体。
- **P2-1**：`cross_room_resolution` 改为不透明 `N7` + `given` 声明实体归属表（不再把 Room 编码进 netEntityId）。
- **P2-2**：`resolveByConnection.result` 与 `server_resolves_admitted_connection.then` 对齐为 `binding.record` + 当前权威 revision。
- **P2-3**：`wordingAudit.exemption` 声明 u64 为位宽记法豁免（hello-wire-v1 先例）；ADR 决策第 6 条同步。
- **P2-4**：示例声明改为低敏 `EntityIdentity.entityType`；正例 given / scope_violation payload 同步，不再把 `accountId` 标成 room-public。
- **多违例单码裁决**（上一轮 429 中断点）：结构化 `multiViolationTieBreak.codes` = `invalid_binding_shape > scope_violation > cross_room_reference > binding_not_found > attributeIdClassification`，`requestErrorBeforeOutcome=true`。对应 invalidCase `multi_violation_shape_outranks_scope_and_storage` 期望 `invalid_binding_shape`。识别器「更严格」不得改判已钉死的 `expectedRejection`（SQL 必须是 `invalid_attribute_id`）。

invalidCases 由 8 增至 10（路径分隔符 + 多违例）；testCases 仍 10，五结局覆盖不变。

## 二、验证证据（worktree 内实测）

1. `node -e <自检脚本>`（JSON.parse、classification.order 三步、failure.variants 双变体、五结局覆盖、invalidCases expectedRejection 均在码表、文法×识别器交叉、u64 豁免、tie-break 数组）：

```
JSON.parse: OK bytes=19095
classification.order: 3 steps
storageRecognizerFeatures: 3
multiViolationTieBreak: invalid_binding_shape > scope_violation > cross_room_reference > binding_not_found > attributeIdClassification
failure.variants: outcomeFailure,requestError
testCases: 10 (ok: 4 outcomes: non_existent=1 stale_generation=2 invisible=1 unauthorized=1 tombstoned=1)
invalidCases: 10
invalidCase codes: arbitrary_property_name_lookup=>invalid_attribute_id; sql_expression_payload=>invalid_attribute_id; storage_path_addressing=>storage_access_forbidden; undeclared_attribute_id=>undeclared_attribute; cross_room_resolution=>cross_room_reference; client_reads_persist_only_attribute=>invisible; binding_record_carries_account_entity_reference=>invalid_binding_shape; client_invokes_server_authoritative_scope=>scope_violation; storage_path_separator_addressing=>storage_access_forbidden; multi_violation_shape_outranks_scope_and_storage=>invalid_binding_shape
classifier SQL: invalid_attribute_id storage: storage_access_forbidden path: storage_access_forbidden
SELF-CHECK: OK
```

2. `node .spec/tools/spec-lint.mjs`：

```
spec-lint: 1 处不一致

  ✗ .spec\decisions\ADR-053-entity-binding-and-attribute-query.md: 未登记进 decisions/README.md 索引
```

**预期中的唯一内容性失败**（四张 Wave 0 契约卡并行，README 属共享文件，登记由主 loop 串行合并）。

3. `node eng/generate-abi.mjs`：

```
ABI_VERSION=1
ENTRY_SYMBOL=lumio_engine_get_api_v1
DEFINITION_SHA256=1dfc86dad1ebbd8d6196d16946a9eb8542e951c83fa5e6163f696abee831fb8e
```

执行前后 `git status --short -- engine/abi` 与 `git diff --stat -- engine/abi` 均为空 → **零差异**。

4. 提交卫生：`git add` 仅两契约文件；`git status --short` 无 120000→100644 mode-only；未 `git add -A`；未 push。

## 三、known gaps

1. `decisions/README.md` 索引登记 + `docs/adr` symlink + `engine/wire/README.md` 导航行——主 loop 串行合并时补（禁改共享文件）。
2. ADR 编号 053 为暂定占号：合并时主 loop 以双目录并集现查最高号后定终号。
3. 统一校验器 `eng/verify-wire.mjs` 由 C-1（R-00355）并行交付；本卡自检覆盖分类器与码表闭合，最终以统一校验器执行全部内嵌用例为准。
4. 属性声明表具体条目随 R-00355 冻结；本契约只定义声明结构、三维词表与读取规则。
5. 存储路径分隔符用例只覆盖 `/`，未另给 `\` 独立 fixture（识别器特征已列两项分隔符；实现按 containsAny 处理即可）。
6. 多违例用例钉死的是最高优先级 `invalid_binding_shape`（同时含 scope + storage）；未为裁决链每一个相邻对都给独立 fixture。

## 四、知识沉淀落点

决策已落在 `.spec/decisions/ADR-053-entity-binding-and-attribute-query.md`（本卡产物本体）。本次为审查修复，无新流程/新模式，无需额外 `knowledge/` 沉淀。
