# R-00358 / C-4 交回物报告 — Native Timer ABI 与双层定时契约冻结

- Status: DONE
- 分支：`rm00011/c4-timer-abi`
- Worktree：`C:\Work\LumioGames\LumioGameEngineArchitecture\.claude\worktrees\agent-a82ea50117517fd72`
- Commit：`df764f9`（df764f962f68d0eefd2cd4e4b466ae116d5f364c）
- 基线：origin/main d012c5c

## 一、改动清单

| 文件 | 类型 | 内容 |
|---|---|---|
| `.spec/decisions/ADR-055-native-timer-abi.md` | 新（52 行） | 八段齐全的 Accepted ADR（2026-09-01，引 Room Review Rulings 裁决 #6）：契约唯一真值、opaque Index+Generation+Context 句柄纪律、CallbackSlot 队列化投递模型（无函数指针/无语言原生回调类型进 ABI/不直调热路径）、双层职责表、终态统一三阶段与迁移边界、失败语义。兄弟契约一律用卡号（R-00350/R-00352/R-00272/R-00359）引用，无 ADR 号互引（ADR-006/017 为卡内点名的纪律来源除外）——暂定号 055 若合并时被占，重编号只需改文件名与标题行 |
| `engine/wire/native-timer-abi-v1.json` | 新（265 行） | contractId `lumio.native-timer-abi.v1`。15 个顶层节：layers（hostTimerService / nativeTickFrameTimerManager / unification 三阶段+迁移边界）、fieldSemantics（9 条）、types（TimerHandle/TimerScope/TimerKind/CallbackSlot 生命周期与约束）、operations（scheduleOneShot/scheduleRepeating/cancel/advance）、sharedTypes（FiringRecord）、errorCodes（12）、limits、testCases（4 正例）、invalidCases（9 反例）、consumers、boundaryExclusions（6 条） |

未触碰（按文件集边界）：`.spec/decisions/README.md`、`docs/adr/`、`engine/wire/README.md`、`engine/wire/hello-wire-v1.json`、`eng/`、`engine/abi/**`。

## 二、验证证据（真实命令与输出）

1. `node .spec/tools/spec-lint.mjs` → `spec-lint: 1 处不一致`：
   `✗ .spec\decisions\ADR-055-native-timer-abi.md: 未登记进 decisions/README.md 索引`
   ——**预期项**：README 索引由主 loop 在串行合并时统一登记（四卡并行共写该文件会冲突，卡边界禁改）；登记后 lint 即过。ADR 正文本身的格式检查无其它报错。
2. `node eng/generate-abi.mjs` → 输出 ABI_VERSION=1 / ENTRY_SYMBOL=lumio_engine_get_api_v1 / DEFINITION_SHA256=1dfc86…；随后 `git status --short eng/ engine/abi/` 为空 → **零差异** ✓（Timer 为进程内 API 契约，未扩二进制 ABI）。
3. 契约自检脚本（node -e）→ `SELF-CHECK OK: keys=15, cases=13 (4 positive / 9 negative), classes=one-shot,repeating,cancel,stale-handle,late-completion,slot-failure, errorCodes=12, cross-refs consistent`——卡验收项 2 要求的六类生命周期/失败用例各 ≥1 全覆盖，invalidCases 的 expectedRejection 与 errorCodes 词表交叉一致，无兄弟 ADR 号引用。
4. 破坏探针（TDD 等价物，同一校验逻辑对内存副本）：
   - probe-1（删 late-completion 类用例）→ `missing-case-classes:late-completion`（拒绝）
   - probe-2（引用不存在的错误码）→ `unknown-rejection-code:no_such_code`（拒绝）
   - probe-3（删 limits 键）→ `missing-keys`（拒绝）
   - control（未变异）→ `OK`
5. 提交纪律：`git status --short` 仅两个新增 untracked 文件，无 120000→100644 mode-only 条目；逐文件 `git add`（未用 add -A）。
6. 未执行：`eng/dev-run.ps1`（worktree 下兄弟仓相对路径断链，按派遣口径由主 loop 在主工作区合并时统一跑）；`eng/verify-wire.mjs`（由并行卡 R-00355/C-1 建立，本分支基线尚无，合并后纳入统一校验）。

## 三、Known gaps

1. `decisions/README.md` 索引登记 + `docs/adr/` symlink（含顺带修复既有缺口 ADR-045/ADR-052 两链接）——主 loop 串行合并时处理；本卡 spec-lint 唯一失败项即源于此。
2. ADR 编号 055 为暂定占号：主 loop 合并时须按 `.spec/decisions/` 与 `docs/adr/` 双目录并集现查最高号后确定（重编号只需改文件名 + ADR 标题行 + 状态行首句，正文无其它自引）。
3. R-00352 要求的「服务器侧至少一个周期任务」具体任务未在本契约点名（由 R-00352 选定后回填 consumers.serverPeriodicTask.shape 置信；本契约已冻结其分层归属为 Server Timer Manager 的 Tick/Frame 域）。
4. Bot 发言节奏的 N 值由 R-00352 冻结（本契约只冻结 repeating 语义与 minIntervalTicks=1）。

## 四、沉淀落点

本卡即沉淀本体：架构决策 → `.spec/decisions/ADR-055-native-timer-abi.md`；契约语义与用例 → `engine/wire/native-timer-abi-v1.json`。无需额外 knowledge/ 沉淀（无新模式/新规范引入；双层分层记录已随 ADR 落档）。

## 卡验收项对照（R-00358 三条）

1. 「ABI 文档与契约条目按 ADR-006/017 纪律合并，含 Baseline 与镜像」→ 开发态等价路径已履行：ADR（Accepted）+ engine/wire 契约 + generate-abi 零差异；Baseline/镜像体系已删（59866ec），由 ADR→契约→用例→主 loop 合并登记的等价顺序替代（Owner 2026-09-01 裁定）。
2. 「生命周期与失败 fixtures 合并（one-shot、repeating、cancel、stale handle、late completion、slot failure）」→ 内嵌 testCases/invalidCases 六类全覆盖、逐例确定性断言（'observable and safe' 已按 Room Review 口径替换为确定性用例 + 逐例断言）。
3. 「分层与统一方向被 R-00350 和 R-00352 引用」→ 契约 layers/unification/consumers 三节以卡号记录归属（R-00350 五分钟窗口=宿主层；R-00352 消费者=Native Manager 双侧）；实现性引用发生在两张实施卡执行时。
