# R-00358 / C-4 审查返工交回物 — Timer 失败半径与 errorTriggers

- Status: DONE
- 分支：`rm00011/c4-timer-abi`
- Worktree：`C:\Work\LumioGames\LumioGameEngineArchitecture\.claude\worktrees\agent-a82ea50117517fd72`
- Commit：`d82828c`（d82828c17cf008cfe6d6319b9a7b26b77959e54f）
- 父提交：`df764f9`（原冻结）
- 基线：origin/main d012c5c
- 审查来源：C-4 深审退回（P1-1 fail-stop vs 显式拒绝；P1-2 三码无触发语义；P2 key 计数 / `resolve` / one-shot 失效 / 上限与回退码）

## 一、改动清单

| 文件 | 类型 | 内容 |
|---|---|---|
| `engine/wire/native-timer-abi-v1.json` | 改 | 去掉 `CallbackSlot.constraints` 尾逗号（JSON 非法 → 可 parse）。`errorTriggers` 覆盖全部 12 个稳定错误码（trigger + distinctFrom + blastRadius + kind）。补 7 个 invalidCase：`slot_queue_full`（定时器终态、Manager 继续、进程不退出）、`scope_invalid`、`slot_dispatch_mismatch`、`manager_shutdown`、schedule 面 `invalid_due_tick`、`advance` 回退、`maxSchedulesPerTick`。删除未定义 `resolve` 操作引用；one-shot 句柄失效写入 `fieldSemantics.timerHandle`。`maxSchedulesPerTick` 与 advance 回退映射到既有码（`schedule_budget_exceeded` / `invalid_due_tick`），判别写在 `errorTriggers`。Manager shutdown 进入路径与四操作拒绝从 ADR 迁入 JSON（非第五个公开 ABI）。 |
| `.spec/decisions/ADR-055-native-timer-abi.md` | 改 | 决策 3：队列满由 fail-stop 改为稳定拒绝（定时器终态、Manager 继续、不升级进程失败）。决策 6 / 失败语义 ②：`slot_queue_full` 归稳定拒绝族；③ 明确 generation 溢出是词表外唯一 ProcessFault。`manager_shutdown` 指向契约 `errorTriggers`。验证节同步 16 反例 + 12/12 triggers。 |

未触碰（按文件集边界）：`.spec/decisions/README.md`、`docs/adr/`、`engine/wire/README.md`、`engine/wire/hello-wire-v1.json`、`eng/`、`engine/abi/**`。未 `git add -A`。未 push。

审查条目对照：

- **P1-1** 收口为：`slot_queue_full` = 该触发终态拒绝 + 定时器终态 + 诊断事件 + Manager 继续；**不是**进程 fatal。三处对齐：`fieldSemantics.callbackSlot`、`types.CallbackSlot.constraints`、ADR-055 决策 3 + 失败语义 ②。invalidCase `slot_queue_full_timer_terminal_process_lives` 钉死可观测行为。
- **P1-2** `errorTriggers` 12/12，含与兄弟码判别。`scope_invalid` / `slot_dispatch_mismatch` / `manager_shutdown` 各有 invalidCase。`manager_shutdown` 行为从 ADR 迁入 JSON。
- **P2-1** 顶层键如实计数：本提交 **keys=17**（原交付实际 16，原报告误写 15；本轮新增 `errorTriggers`）。
- **P2-2** 用例不再引用 `resolve`；one-shot 触发后句柄失效在 `fieldSemantics.timerHandle`。
- **P2-3** `maxSchedulesPerTick` → `schedule_budget_exceeded`（面 b）；`advance` 回退 → `invalid_due_tick`（面 b）。未扩 12 码词表。
- **P2-4** 不在本返工范围（源文档交付链措辞；交主 loop）。

## 二、验证证据（真实命令与输出）

1. JSON parse + 自检（`node` 读 `engine/wire/native-timer-abi-v1.json`）：

```
SELF-CHECK OK: keys=17 [contractId,version,purpose,kind,hostAgnosticWording,layers,fieldSemantics,types,operations,sharedTypes,errorCodes,errorTriggers,limits,testCases,invalidCases,consumers,boundaryExclusions], cases=20 (4 positive / 16 negative), classes=one-shot=1,repeating=1,cancel=2,stale-handle=2,late-completion=1,slot-failure=4, extraClasses=determinism,input-validation,shutdown,budget,fail-stop, errorCodes=12, triggers=12, ops=scheduleOneShot,scheduleRepeating,cancel,advance, trailingComma=no
```

六类生命周期/失败用例仍各 ≥1；12 码各有非空 `trigger` + `distinctFrom`；无尾逗号；`operations` 无 `resolve`。

2. `node .spec/tools/spec-lint.mjs` → `spec-lint: 1 处不一致`：

```
✗ .spec\decisions\ADR-055-native-timer-abi.md: 未登记进 decisions/README.md 索引
```

——**预期项**（卡边界禁改 README 索引）。无其它 spec-lint 失败。

3. `node eng/generate-abi.mjs` →

```
ABI_VERSION=1
ENTRY_SYMBOL=lumio_engine_get_api_v1
DEFINITION_SHA256=1dfc86dad1ebbd8d6196d16946a9eb8542e951c83fa5e6163f696abee831fb8e
```

随后 `git status --short -- engine/abi engine/native engine/managed eng` 为空 → **零差异**（SHA 与原冻结报告逐位一致）。

4. 提交纪律：`git add --` 仅上述两路径；`git diff --cached --summary` 无 120000→100644 mode 噪声；`git status --short` 在提交后仅剩 untracked `.wf-report-R-00358.md`（原冻结报告，未纳入本提交）。

5. 未执行：`eng/dev-run.ps1`（worktree 下兄弟仓相对路径断链，按派遣口径由主 loop 在主工作区合并时统一跑）；`eng/verify-wire.mjs`（并行卡 R-00355/C-1）。

## 三、Known gaps

1. `decisions/README.md` 索引登记 + `docs/adr/` symlink —— 主 loop 串行合并时处理；本卡 spec-lint 唯一失败项即源于此。
2. ADR 编号 055 仍为暂定占号（与原冻结相同）。
3. R-00352 服务器周期任务具体点名、Bot N 值仍由实施卡回填（本契约只冻结分层与 repeating 语义）。
4. 上限/回退共用既有错误码、靠 `errorTriggers` 分面，而不是新增第 13/14 码。若主 loop 希望独立码，需另开词表变更。
5. P2-4（Baseline 等价链归因无落档裁定）交主 loop，本返工未改审查文档。

## 四、沉淀落点

本返工是同一冻结卡的契约文本修复：决策修正落在已有 `.spec/decisions/ADR-055-native-timer-abi.md`；触发语义与用例落在已有 `engine/wire/native-timer-abi-v1.json`。无新模式/新规范，不另写 `knowledge/`（纯修复豁免）。

## 卡验收项对照（R-00358 三条，相对审查退回点）

1. ABI/契约按 ADR-006/017 纪律冻结；开发态等价路径仍为 ADR（Accepted）+ `engine/wire` 契约 + generate-abi 零差异。
2. 六类生命周期/失败 fixtures 仍全覆盖；失败词表现在 12/12 有触发语义，`slot_queue_full` 失败半径与 ADR 一致。
3. 分层与统一方向未改；R-00350 / R-00352 卡号引用仍在。
