# R-00357 / C-3 审查修复交回报告 — admission credential framing + ADR-042 digest input

- Status: DONE
- 分支：`rm00011/c3-account-port`（基点 76ae971 + 本修复提交）
- Worktree：`C:\Work\LumioGames\LumioGameEngineArchitecture\.claude\worktrees\agent-adfa36696ef8d195a`
- Commit：`e8a758f` — fix(wire): define admission credential framing and ADR-042 digest input (R-00357)
- 日期：2026-09-01
- 对照审查：深审报告 P1-1 / P1-2 / P2（5 项）全部落盘；未重起草契约

## ① 改动清单（2 文件，+12 / −8）

| 文件 | 性质 |
| --- | --- |
| `engine/wire/account-port-v1.json` | 审查修复：凭证 framing、digest 口径、keyId 取舍、Bot preimage、grammar 注、删除悬空 `clientName` |
| `.spec/decisions/ADR-054-account-server-topology-and-port.md` | P2-5：背景节 `account-server//` → `account-server/` |

逐条对照审查 Issues（grep + 通读确认，无残留旧措辞）：

| # | 项 | 落点 |
| --- | --- | --- |
| P1-1 | digest 注文与 ADR-042 §2 / 自身 preimage 矛盾 | `admissionCredential.digest` 改为「摘要以 64 小写 hex 拼入 preimage 参与验签；32 字节是摘要本体而非签名输入」。全文已无「原始 32 字节摘要作验签输入；hex 仅供展示」 |
| P1-2 | `credentialBytes` 组合布局未定义 | `admissionCredential.framing`：`credentialBytes = LumioBinV1(canonicalPayload) \|\| rawSignature(64 字节)`；容器内签名固定 raw 64 字节，hex 只展示。`verify_admission` 解形语义同步引用该布局 |
| P2-1 | `keyId u8` vs ADR-042 §3 派生串 | `keyManagement.keyIdNote`：采纳 payload 内 u8 轮换位，**不采纳** ADR-042 §3 |
| P2-2 | `botToolCredential.signature` 无 preimage/digest | 已补 `digest` + `preimage`（`… bot-tool … bot-tool-credential-v1 … <payloadDigest-hex>`），并补 `format.framing` 与 admission 同口径 |
| P2-3 | `invalid_username_grammar` 注算术错 | `given`：`loginName="x"（长度 1 < loginNameMinLength=3）` |
| P2-4 | 悬空 `clientName` | 已从 `login_or_register.request.optional` 删除；account-port JSON 内 0 命中 |
| P2-5 | ADR 双斜杠 | 背景节 `account-server/`；决策 1 处原本正确 |

JSON.parse 通过。未改 README / index / docs/adr / hello-wire / eng / abi。

## ② 验证证据（本轮实跑）

```
$ node -e "JSON.parse(require('fs').readFileSync('engine/wire/account-port-v1.json','utf8')); console.log('JSON.parse OK')"
JSON.parse OK    exit=0
```

```
$ node .wf-selfcheck-r00357.mjs
contract: engine/wire/account-port-v1.json
errorCodes: 14, errorCodeSemantics: 14
testCases: 7, invalidCases: 11
SELF-CHECK OK    exit=0
```

自检已扩展审查不变量：framing 必须是 LumioBinV1(payload)||rawSignature(64)、preimage 含 `<payloadDigest-hex>`、digest 引用 ADR-042 §2 且禁止裸摘要措辞、keyIdNote 声明不采纳 §3、clientName 不得出现、grammar 注必须写长度 1。

```
$ node .wf-selfcheck-r00357.mjs engine/wire/account-port-v1.json --probe
PROBE codes: 3 detection(s) expected >0
  DETECTED: errorCodeSemantics key not in errorCodes: bot_tool_credential_expired
  DETECTED: operations.login_or_register.failureCodes unknown code: bot_tool_credential_expired
  DETECTED: invalidCase bot_tool_credential_expired expectedRejection not in errorCodes: bot_tool_credential_expired
PROBE framing/preimage: 6 detection(s) expected >0
  DETECTED: admissionCredential.framing missing or not LumioBinV1(payload)||raw-64-byte form
  DETECTED: botToolCredential.format.framing missing or not LumioBinV1(payload)||raw-64-byte form
  DETECTED: admissionCredential.signature.preimage missing or lacks payloadDigest-hex
  DETECTED: botToolCredential.signature.preimage missing or lacks payloadDigest-hex
  DETECTED: admissionCredential.digest must cite ADR-042 §2 (hex-in-preimage)
  DETECTED: admissionCredential.digest still contains the contradictory bare-digest phrasing
probe exit=0
```

```
$ node .spec/tools/spec-lint.mjs
spec-lint: 1 处不一致
  ✗ .spec\decisions\ADR-054-account-server-topology-and-port.md: 未登记进 decisions/README.md 索引
spec-lint exit=1
```
→ 恰好 1 条且为**预期**：文件集边界不改 `decisions/README.md`（四卡并行防冲突），登记由主 loop 串行合并时统一做。

```
$ node eng/generate-abi.mjs
ABI_VERSION=1
ENTRY_SYMBOL=lumio_engine_get_api_v1
DEFINITION_SHA256=1dfc86dad1ebbd8d6196d16946a9eb8542e951c83fa5e6163f696abee831fb8e
exit=0
$ git status --short engine/abi engine/native engine/managed eng
（无输出 = 生成物零差异）
```

```
$ node eng/verify-hello-wire.mjs
tests 9 / pass 9 / fail 0 / cancelled 0 / skipped 0
HELLO_WIRE_OK    exit=0
```

- 提交：`git add --` 仅 ADR-054 + `engine/wire/account-port-v1.json`；未 `git add -A`；无 symlink mode 噪声。
- 自检脚本 `.wf-selfcheck-r00357.mjs` 仍为 worktree 本地未跟踪文件（探针路径保留），不入库。
- `dev-run.ps1` 未在 worktree 执行（兄弟仓相对路径断链）；纯文档+JSON 修复，按原交付口径由主 loop 合并时跑。

## ③ Known gaps / 偏离

1. **ADR 编号 054 暂定**（与原交付相同）：合并时主 loop 双目录现查最高号再定。
2. **decisions/README.md 索引与 docs/adr symlink 未登记**：边界内故意留给主 loop；spec-lint 因此保持 1 条预期失败。
3. ADR-042/047 仍为 Draft：本契约是其正式消费者；keyId 明确不采纳 §3，trustDomain `account-admission` / `bot-tool` 仍在 ADR-042 枚举之外（切片取舍已在 `keyIdNote` 写明）。
4. 契约统一机器校验（`eng/verify-wire.mjs`）仍由 C-1 并行交付；本卡 worktree 无该脚本。
5. 审查方案疑虑 (a)(b)(c) 交主 loop 裁决，本修复不改口径：Baseline/七仓镜像按预上线 Living Architecture 豁免；「一账号同时只在一个 Room」仍不在本契约 / outOfScope；ADR 占号仍 054。

## ④ 沉淀落点

- 纯审查修复 / 机械对齐已有 ADR-042 §2 与本卡既有 preimage，无需额外 `knowledge/` 沉淀（豁免声明）。
- 决策真值仍是 ADR-054；字段真值仍是 `engine/wire/account-port-v1.json`。
